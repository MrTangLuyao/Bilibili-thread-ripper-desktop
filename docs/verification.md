# 验证记录

## 0.9.1.1-d1 发布接口

日期：2026-09-16。本次增加桌面版编号、GitHub 更新清单和一键安装，不更换下载器和原生播放器。

本次完成的自动化检查：

- Windows PowerShell 5.1 通过 IEX 加载安装脚本，在隔离的模拟客户端完成首次安装、重复安装和还原。安装包由本地待发布文件提供，未改动正在使用的客户端。
- 安装包 SHA-256 不匹配时，关闭客户端和修改官方资源之前就停止。
- 重复安装更换安装路径，仍从原始备份生成补丁，不会一层层叠加。还原后的文件逐字节等于原文件。
- 拒绝越界 ZIP 路径、未知客户端版本和不属于本仓库的下载地址。
- 用同一下载函数实际读取 GitHub 仓库的 LICENSE，验证 HTTPS 下载和大小限制。
- 版本号相同显示最新；任意不同的完整版本号显示有更新，包括桌面修订变化、主版本变化和回退。
- 更新 IPC 只接受官方客户端主页面和播放页面的顶层窗口。页面脚本伪造点击不会启动安装；真正点击后仍有原生确认框。
- 取消安装、安装脚本被修改、重复点击的处理已检查。更新进程只执行本地已校验的安装脚本，不接收页面传入的命令或下载地址。
- 浏览器自动化检查系统设置首项、Debug 过滤、持久化、主窗口与播放器设置同步、更新按钮及错误后的重试。
- 共享下载器完成 1 MiB 多线程下载，组合后的字节完全一致；XHR 生命周期、复用请求、路由切换取消、失败回退和 fetch 响应地址回归通过。

这里的安装测试使用模拟客户端，更新检查使用受控清单。GitHub 的正式 install.ps1、latest.json 和 ZIP 需要维护者 commit、push 后才存在，尚未将“从线上仓库安装新版”记为通过。

## 桌面适配基础版本的实际客户端记录

日期：2026-09-15。官方客户端 1.18.0，基于 BTR 0.9.1.1 的桌面适配修订 1。以下是此前在实际客户端完成的检查，不是本次 d1 发布接口的在线验收。

- 实际完成安装、还原、再次安装，原始 app.asar 的 SHA-256 一致。官方更新缓存的资源包保持原始哈希，没有关闭完整性校验。
- 原始包中只增加 index.js 的 BTR 启动前缀，其余原始资源保持不变。BTR 自己的资源另外添加。
- 系统设置在常规设置前显示线程撕裂者；播放器齿轮菜单的“更多播放设置”保留 CDN 和线程数。两边同步，重启后保留，切换分 P 后仍能使用。
- BV1R1e4zKEh1 固定 4K，从约 17 分钟拖回 5 分钟后约 7 秒恢复，最终 3840×2160、readyState 4。之后连续约 60 秒，时间从 370.06 秒推进到 430.38 秒，没有新增超时和回退。不宣称跳转没有等待。
- 在 4K 合集中连续切换视频，包括加载期间再次切换。最终 BV1BbULBbEZM、CID 34162544531 连续播放约 20 秒，3840×2160、readyState 4，仍有 BTR 下载，无媒体错误。
- 在 BV1ps4y1d73V 中连续切换分 P，确认 BV 不变而 CID 改变，最终一 P 正常播放、readyState 4、接管请求持续完成，无媒体错误。

shared 下五个文件仍与浏览器版对应代码完全一致。官方客户端未来版本、直播和离线下载不在本次适配验证范围内。以上结果不代表所有资源和网络条件下都不会卡顿。
